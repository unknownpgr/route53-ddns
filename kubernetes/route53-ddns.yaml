apiVersion: apps/v1
kind: Deployment
metadata:
  name: route53-ddns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: route53-ddns
  template:
    metadata:
      labels:
        app: route53-ddns
    spec:
      containers:
        - name: route53-ddns
          image: unknownpgr/route53-ddns:5db8552
          imagePullPolicy: Always
          env:
            - name: RECORD
              value: "*.server.unknownpgr.com;*.unknownpgr.com;*.ction.link;univting.com;*.univting.com"
            - name: TTL
              value: "300"
          volumeMounts:
            - name: secret
              mountPath: "/app"
              readOnly: true
          stdin: true
          tty: true
      volumes:
        - name: secret
          secret:
            secretName: route53
            optional: false
